# HEATMAP Releases Page

This branch (`gh-pages`) contains the source code for the [HEATMAP Releases Page](https://NagusameCS.github.io/HEATMAP/).

The `index.html` file uses the GitHub API to fetch and display the latest releases from the repository.
